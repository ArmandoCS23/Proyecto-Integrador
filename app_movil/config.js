/**
 * Configuración global de la app móvil
 * Reemplaza la URL del servidor por la IP de tu PC en la red local
 */

export const SERVER_URL = "http://192.168.100.66:5000"; //remplaza con tu IP real (incluye puerto 5000 si el backend usa ese puerto)